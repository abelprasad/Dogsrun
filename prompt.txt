Add a sign out button to both the shelter dashboard and rescue portal navbar.

## Shelter Dashboard (src/app/dashboard/page.tsx)
In the navbar, add a sign out button on the right side next to the org name/avatar. It should:
- Use the Supabase browser client from '@/lib/supabase'
- Call supabase.auth.signOut() then redirect to '/'
- Since this is a server component, the sign out button needs to be a separate client component
- Create src/app/dashboard/sign-out-button.tsx as a 'use client' component that handles the sign out logic
- Style: text-sm font-medium text-gray-500 hover:text-gray-900 transition-colors

## Rescue Portal (src/app/dashboard/rescue/page.tsx)
Same approach — import and use the same SignOutButton component in the rescue portal navbar.

## SignOutButton component (src/app/dashboard/sign-out-button.tsx)
'use client'
- Import createClient from '@/lib/supabase'
- On click: await supabase.auth.signOut() then window.location.href = '/'
- Render as a simple text button matching the design system