I'm building a Next.js 15 app with Supabase. I need to make the rescue criteria page fully editable.

File to edit: src/app/dashboard/criteria/page.tsx

The rescue_criteria table schema:
- id (uuid)
- rescue_id (uuid → organizations.id)
- breeds (text[])
- max_age_years (integer)
- max_weight_lbs (integer)
- sex_preference (text: 'male'|'female'|'any')
- accepts_mixes (boolean)
- states_served (text[])
- is_active (boolean)
- created_at

The org lookup always uses .eq('email', user.email) NOT .eq('id', user.id) because organizations.id is separate from the Supabase auth user UUID.

Requirements:
- If no criteria exist, show a form to create them
- If criteria exist, show the current values with an Edit button
- Editable fields: breeds (comma-separated input that saves as array), max_age_years, max_weight_lbs, sex_preference (dropdown: any/male/female), accepts_mixes (checkbox), states_served (comma-separated), is_active (toggle)
- On save, upsert to rescue_criteria table
- Use the existing design system: white bg, amber #f59e0b accents, gray-900 text, rounded-xl cards, Tailwind CSS
- Use the Supabase browser client from @/lib/supabase