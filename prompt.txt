In C:\code\fillr\popup, make these changes to the resume section UI:
popup.html — replace the entire <div class="resume-section" ...> block with:
html<div class="resume-section" id="resumeSection">
  <input type="file" id="resumeFileInput" accept=".pdf" style="display:none">
  <div class="resume-drop-zone" id="resumeDropZone">
    <svg width="14" height="14" viewBox="0 0 16 16" fill="none" style="flex-shrink:0"><path d="M3 2h7l3 3v9H3V2z" stroke="currentColor" stroke-width="1.2" stroke-linejoin="round"/><path d="M10 2v3h3" stroke="currentColor" stroke-width="1.2"/></svg>
    <span>Upload resume (PDF)</span>
  </div>
  <div class="resume-pill hidden" id="resumeStatus">
    <svg width="14" height="14" viewBox="0 0 16 16" fill="none" style="flex-shrink:0"><path d="M3 2h7l3 3v9H3V2z" stroke="currentColor" stroke-width="1.2" stroke-linejoin="round"/><path d="M10 2v3h3" stroke="currentColor" stroke-width="1.2"/></svg>
    <span class="resume-filename" id="resumeFilename"></span>
    <button class="resume-change-btn" id="resumeClearBtn">change</button>
  </div>
  <div class="resume-processing hidden" id="resumeProcessing">
    <span>Extracting text...</span>
  </div>
</div>
popup.css — remove all existing .resume-* styles and replace with:
css.resume-section { margin: 8px 0 4px; }

.resume-drop-zone {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 12px;
  border: 1px dashed var(--border, #4a5568);
  border-radius: 8px;
  cursor: pointer;
  font-size: 12px;
  color: var(--text-secondary, #9ca3af);
  transition: border-color 0.15s, color 0.15s;
}
.resume-drop-zone:hover { border-color: var(--primary, #2563eb); color: var(--text, #f1f5f9); }
.resume-drop-zone.drag-over { border-color: var(--primary, #2563eb); }

.resume-pill {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 12px;
  background: var(--surface, #1e2a3a);
  border: 1px solid var(--border, #2d3748);
  border-radius: 8px;
  font-size: 13px;
  color: var(--text, #f1f5f9);
}

.resume-filename {
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  font-size: 13px;
}

.resume-change-btn {
  background: none;
  border: none;
  cursor: pointer;
  font-size: 11px;
  color: var(--text-secondary, #9ca3af);
  padding: 0;
  flex-shrink: 0;
}
.resume-change-btn:hover { color: var(--text, #f1f5f9); }

.resume-processing {
  padding: 8px 12px;
  font-size: 12px;
  color: var(--text-secondary, #9ca3af);
}

.hidden { display: none !important; }
popup.js — in showResumeUploadArea, replace document.getElementById('resumeUploadArea') with document.getElementById('resumeDropZone') everywhere. Also in handleResumeUpload, replace the line document.getElementById('resumeUploadArea').classList.add('hidden') with document.getElementById('resumeDropZone').classList.add('hidden'). Do the same in setupResumeUpload — replace resumeUploadArea references with resumeDropZone. The clearBtn click handler should call showResumeUploadArea() which shows the drop zone again, not the upload area div.
Then reload the extension in Brave.